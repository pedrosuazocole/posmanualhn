[build]
builder = "nixpacks"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[deploy.healthChecks]]
type = "http"
path = "/api/licencia/estado"
interval = 30
timeout = 10
